# CustomerSegmentation
K-Means Customer Segmentation: Complete ML pipeline with EDA, feature engineering, WCSS analysis &amp; sklearn KMeans clustering. Deployed as interactive Streamlit app. Includes trained model .pkl files via pickle for instant inference. Production-ready code from data prep to deployment.
